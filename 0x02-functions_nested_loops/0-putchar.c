#include <stdio.h>
#include <stdlib.h>
#include <main.h>
/**
 * main - prints _putchar, followed by a new line.
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char line;

	for (line = "_putchar");
	_putchar(line)
	_putchar('\n')
	return (0);
}
