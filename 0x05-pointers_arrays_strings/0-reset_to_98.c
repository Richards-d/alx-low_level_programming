#include "main.h"
/**
 * main - takes a pointer to an int as parameter and updates the value it
 * points to to 98.
 */
void reset_to_98(int *n)
{
	int var = 98;
	int *n;

	_putchar("Value of *n variable: %d\n, *n");
	return (0);
}
